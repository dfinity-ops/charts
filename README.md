# charts

To pull the helm chart, use one of these two registries:
```
helm pull oci://registry.gitlab.com/devopscoop/charts/app --version 0.6.0
helm pull oci://ghcr.io/devopscoop/charts/app --version 0.6.0
```
